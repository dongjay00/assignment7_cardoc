//경로 변수들 모음

// Root
const ROOT = '/';

// User
const USER_SIGNUP = '/user';
const USER_SIGNIN = '/token';


const routes = {
  root: ROOT,
  user: USER_SIGNUP,
  token: USER_SIGNIN,
}

module.exports = routes;